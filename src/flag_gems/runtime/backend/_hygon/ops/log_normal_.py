# Copyright 2026, The FlagOS Contributors.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# Generated by KernelGen: https://github.com/flagos-ai/KernelGen

import logging

import triton
import triton.language as tl

from flag_gems.runtime import torch_device_fn
from flag_gems.utils.random_utils import philox_backend_seed_offset
from flag_gems.utils.shape_utils import volume

logger = logging.getLogger(__name__)

BLOCK_SIZE = 256


@triton.jit(do_not_specialize=["philox_seed", "philox_offset"])
def log_normal_kernel(
    out_ptr,
    n_elements,
    mean,
    std,
    philox_seed,
    philox_offset,
    BLOCK_SIZE: tl.constexpr,
):
    # Single-pass log-normal sampler generated by KernelGen for the Hygon
    # backend: draw standard normals with tl.randn, then map through
    # exp(mean + std * normal). The Philox seed/offset from the FlagGems RNG
    # state make the stream generator-aware while keeping the kernel algorithm
    # identical to the eval-server-verified candidate.
    pid = tl.program_id(0)
    block_start = pid * BLOCK_SIZE
    offsets = block_start + tl.arange(0, BLOCK_SIZE)
    mask = offsets < n_elements

    # Generate standard normal random numbers
    randn_vals = tl.randn(philox_seed, offsets + philox_offset)

    # Compute log-normal: exp(mean + std * normal)
    log_normal_vals = tl.exp(mean + std * randn_vals)

    tl.store(out_ptr + offsets, log_normal_vals, mask=mask)


def log_normal_(self, mean=1.0, std=2.0, *, generator=None):
    logger.debug("GEMS_HYGON LOG_NORMAL_")
    device = self.device
    n_elements = volume(self.shape)

    grid = (triton.cdiv(n_elements, BLOCK_SIZE),)
    philox_seed, philox_offset = philox_backend_seed_offset(
        n_elements, generator=generator
    )
    with torch_device_fn.device(device):
        log_normal_kernel[grid](
            self,
            n_elements,
            float(mean),
            float(std),
            philox_seed,
            philox_offset,
            BLOCK_SIZE,
        )
    return self
